WHENEVER SQLERROR EXIT SQL.SQLCODE

CREATE OR REPLACE FUNCTION C##TMP_USER.FUNC_GET_APP_DESCRIPTION (I_APP_ID IN NUMBER)
	RETURN VARCHAR2
IS
  V_APP_DESCRIPTION C##TMP_USER.APP_TABLE.APP_DESCRIPTION%TYPE;
BEGIN
   BEGIN
	   SELECT A.APP_DESCRIPTION
	   INTO V_APP_DESCRIPTION
	   FROM C##TMP_USER.APP_TABLE A
	   WHERE A.APP_ID = I_APP_ID;

	EXCEPTION
	  WHEN OTHERS THEN
		V_APP_DESCRIPTION := '';
	END;

    RETURN V_APP_DESCRIPTION;
END GET_APP_DESCRIPTION;